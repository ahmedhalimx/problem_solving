#include <time.h>
#include <stdio.h>

int main() {
	time_t t = time(NULL);
	printf("%s\n", asctime(gmtime(&t)));
	printf("%s\n", asctime(gmtime(&t)));
	printf("%s\n", asctime(gmtime(&t)));
	printf("%s\n", asctime(gmtime(&t)));
	printf("%s\n", asctime(gmtime(&t)));
}
